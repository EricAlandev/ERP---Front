

export type UserType = {
    email ?: string;
    password ?: string;
    birthday ?: string;
    token?: string;
}

export type SearchUser = {
    email ?: string;
    idUser ? : string;
    contractType ?: string;
}